# Videos folder

Put your video file here as `demo.mp4`.

All 7 setup steps will use the same `demo.mp4` for now.

To change per-step videos later, rename to `step-1.mp4` ... `step-7.mp4` and update the `videoSrc` logic in `index.html`.
